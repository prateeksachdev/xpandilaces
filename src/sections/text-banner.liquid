<section class="section text-banner">
  <div class="container">
    {% if section.settings.title != blank %}
      <h2>{{ section.settings.title }}</h2>
    {% endif %}

    {% if section.settings.text != blank %}
      {{ section.settings.text }}
    {% endif %}

    {% if section.settings.primary_cta_text != blank %}
      <p class="primary-cta">
        <a class="button" href="{{ section.settings.primary_cta_link }}">
          {{ section.settings.primary_cta_text}}
        </a>
      </p>
    {% endif %}

    {% if section.settings.enable_secondary_cta %}
      <p class="secondary-cta">
        <a class="button" href="{{ section.settings.secondary_cta_link }}">
          {{ section.settings.secondary_cta_text}}
        </a>
      </p>
    {% endif %}
  </div>
</section>

{% schema %}
  {
    "name": "Text Banner",
    "settings": [
      {
        "type": "text",
        "id": "title",
        "label": "Title"
      },
      {
        "type": "richtext",
        "id": "text",
        "label": "Text"
      },
      {
        "type": "text",
        "id": "primary_cta_text",
        "label": "Primary CTA Text"
      },
      {
        "type": "url",
        "id": "primary_cta_link",
        "label": "Primary CTA Link"
      },
      {
        "type": "checkbox",
        "id": "enable_secondary_cta",
        "label": "Enable Secondary CTA"
      },
      {
        "type": "text",
        "id": "secondary_cta_text",
        "label": "Secondary CTA Text"
      },
      {
        "type": "url",
        "id": "secondary_cta_link",
        "label": "Secondary CTA Link"
      }
    ],
    "presets": [
      {
        "name": "Text Banner",
        "category": "Text"
      }
    ]
  }
{% endschema %}